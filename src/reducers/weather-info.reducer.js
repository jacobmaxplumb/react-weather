const initialState = {
    description: '',
    temp: 0,
    hasWeather: true
}

export const weatherInfoReducer = (state = initialState, action) => {
    switch(action.type) {
        default:
            return state;
    }
}

