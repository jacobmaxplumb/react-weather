const initialState = {
    description: '',
    temp: 0,
    hasWeather: false
}

export const weatherInfoReducer = (state = initialState, action) => {
    switch(action.type) {
        default:
            return state;
    }
}

